# CASM WP29 trusted reference: p1size1
#
# Independently hand-assembled from the p1size1 fixture source in
# cmake/GenerateCasmTestFixtures.cmake (originally authored for WP28's Pass 1
# "measure engine" sizing fixtures as its comprehensive sanity check; reused
# here, per the user's confirmed WP29 decision, as the real-emission trusted
# reference for the same program). NOT produced by CASM. Cross-checks:
#   - brain/plans/2026-07-22-casm-phase6-wp28-pass1-address-assignment.md
#     ("p1size1" fixture, hand-verified Pass 1 sizing: final CasmPc = $C010,
#     LOOP = $C003, DATA = $C009, VALS = $C00C)
#   - brain/plans/2026-07-23-casm-phase6-wp29-pass2-resolution-emission.md
#     (Contract item 3, this fixture's real-emission byte derivation)
#
# Source (.ORG $C000):
#   - JMP LOOP
#   - LOOP: LDA #$01
#   - STA $D020
#   - RTS
#   - DATA: .BYTE $01, $02, $03
#   - VALS: .WORD $ABCD, $1234
#
# Combines a forward reference (JMP LOOP, always 3 bytes regardless of
# absolute-width rules), two more backward-referenceable labels (DATA, VALS),
# and both list directives -- the same program WP28 used to prove Pass 1
# sizing end to end, now proving Pass 2's real byte emission matches it.
#
# Hand assembly:
#   00 C0           PRG load-address header ($C000, little-endian)
#   4C 03 C0        JMP $C003         (LOOP, forward reference)
#   A9 01           LDA #$01          (at $C003, defines LOOP)
#   8D 20 D0        STA $D020
#   60              RTS               (at $C008; DATA defined at $C009)
#   01 02 03        .BYTE $01,$02,$03
#   CD AB 34 12     .WORD $ABCD,$1234 (little-endian; VALS defined at $C00C)
#
# bytes: 18
# sha256: 64ea0f391004b4638faedf0d7202bfae704e68623b4e4a0c8ff9e95dab304596

00 C0
4C 03 C0
A9 01
8D 20 D0
60
01 02 03
CD AB 34 12
