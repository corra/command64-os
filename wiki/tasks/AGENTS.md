# Purpose
The purpose of the `wiki/tasks` directory is to contain the detailed requirements and status files for the active, pending, or recently completed tasks and milestones of command64.

# Ownership
- Primary Owner: Companion Agent (Gemini)
- Peer Owner: Primary Architect (Claude)

# Local Contracts
- Each milestone has its own markdown file.
- Keep files updated with `[ ]` (pending), `[/]` (in-progress), and `[x]` (completed) tasks.
- Keep tasks in sync with Taskwarrior and `brain/task.md`.

# Work Guidance
- Use kebab-case filenames for task descriptions.
- Ensure sub-tasks are measurable and testable.

# Verification
- Confirm that Taskwarrior tasks are updated to reflect the state in `wiki/tasks/`.

# Child DOX Index
- (none)
