# Build artifacts (managed by Make)
build/
tests/bin/
tests/*.d64
src/external/debug/build/
src/external/debug/bin/

ms-dos/

# Stray root-level artifacts only (leading / anchors to repo root)
/image.d64
/*.prg
/*.sym
/*.vs
unsloth_compiled_cache/

# Tools
tools/cc1541-git/

# Python bytecode cache
__pycache__/

# OS-specific
.DS_Store
package-lock.json
package.json
node_modules/

# Local tool state (Claude Code settings, MCP index cache). Skills are
# agent-neutral-ish, checked-in tooling (see .claude/skills/*/SKILL.md's
# own note that they're a thin, non-authoritative layer over agent-neutral
# docs) and are tracked; settings.json/settings.local.json stay untracked
# for now pending a pass to split trackable vs. local-only fields.
.claude/*
!.claude/skills/
.codebase-memory.json
